{
  "scripts": {
    "dev:direct": "rm -r -f node_modules && docker compose up -d php && npm i && npx vite --open",
    "dev:docker": "rm -r -f node_modules && docker compose up -d && docker exec -it node npm i && docker exec -it node npx vite --host"
  },
  "dependencies": {
    "vite": "^4.5.0"
  }
}
